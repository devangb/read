<%= form_for(@book) do |f| %>

  
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new review..." %>
  </div>
  
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>